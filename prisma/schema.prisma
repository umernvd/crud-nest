datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

model Post {
  id      Int     @id @default(autoincrement())
  title   String
  content String?
}
